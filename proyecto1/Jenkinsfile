pipeline {
    // Usamos el agente universal que tu Jenkins sí reconoce
    agent any
    
    environment {
        REGISTRY = 'ghcr.io'
        IMAGE_NAME = 'ALalejandra/proyecto1'
    }
    
    stages {
        stage('Prepare') {
            steps {
                echo '⚙️ Validando comandos disponibles...'
                // Usamos comandos de Windows (bat) ya que tu Jenkins corre de forma local
                bat 'node --version'
                bat 'npm --version'
            }
        }
        
        stage('Install Dependencies') {
            steps {
                echo '📥 Instalando dependencias de la app...'
                bat 'npm ci'
            }
        }
        
        stage('Test') {
            steps {
                echo '🧪 Ejecutando pruebas del proyecto...'
                bat 'npm test'
            }
        }
    }
    
    post {
        success {
            echo '🎉 ¡Felicidades! El pipeline se ejecutó y completó con éxito.'
        }
        failure {
            echo '❌ El pipeline tuvo un fallo, revisa los logs de arriba.'
        }
    }
}